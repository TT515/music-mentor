# Server-side (Vercel env vars — never exposed to the browser)
ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_MODEL=claude-sonnet-5
MODAL_BASE_URL=https://YOUR-WORKSPACE--music-mentor-worker-api-app.modal.run

# Client-side (baked into the bundle at build time)
VITE_MODAL_BASE_URL=https://YOUR-WORKSPACE--music-mentor-worker-api-app.modal.run
